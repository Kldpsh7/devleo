# Security

Report vulnerabilities privately through GitHub Security Advisories after the repository is published. Do not open a public issue containing exploit details or user data.

The pet runs without elevated privileges, exposes no TCP listener, performs no screen capture or input logging, and has no telemetry. Its local control channel is user-scoped.

Supported security fixes target the latest released minor version.

