# Render outputs

- `work/` is ignored scratch output.
- `approved/` is reserved for reviewed, reproducible master renders and QA artifacts.
- Runtime sprites remain under `assets/approved` and `src/lion_cub_pet/assets`.

Never copy a Blender render into the runtime asset tree before deterministic QA and
visual approval at all supported display scales.
