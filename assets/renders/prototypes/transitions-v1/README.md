# Leo transition prototype v1

Status: approved isolated Blender prototype; not copied into runtime assets.

Generated from `assets/source-3d/leo.blend` with:

```bash
LEO_TRANSITION_OUTPUT=assets/renders/work/transitions-candidate-5 \
  tools/blender/render_transitions.sh
```

The snapshot retains actual-size previews, master contact sheets, deterministic
QA, background evidence, and the independent visual verdict for 20-frame seated
Idle to Walk transitions in both directions plus their exact reversed returns.

Candidate 5 supersedes earlier local candidates. It registers every frame to
master ground line 1214 and carries the closed laptop on a camera-space exterior
arc with a connected foreleg following the prop.
